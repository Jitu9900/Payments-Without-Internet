workflows:
  android-debug:
    name: Android Debug APK
    instance_type: mac_mini_m2

    environment:
      java: 17

    scripts:
      - name: Build APK
        script: |
          chmod +x ./gradlew
          ./gradlew assembleDebug

    artifacts:
      - app/build/outputs/apk/debug/*.apk
